{
  "name": "jelofullstack",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "concurrently \"cd backend && node server.js\" \"cd client && npm start\"",
    "heroku-postbuild": "cd backend && npm install && npm run begin"

  },
  "dependencies": {
    "concurrently": "^4.0.1",
    "nodemon": "^2.0.2"
  }
}
