{
  "name": "jelofullstack",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "concurrently \"cd backend && node server.js\" \"cd client && npm start\"",
    "heroku-postbuild": "cd client && npm install && cd .. && cd backend && npm install && cd .. && npm install && concurrently \"cd backend && node server.js\" \"cd client && npm start\""

  },
  "dependencies": {
    "concurrently": "^4.0.1",
    "nodemon": "^2.0.2"
  }
}
